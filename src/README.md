# src/ — Original TLS v1 Calculator

This is the original TLS calculator that produced the results 
in the Year 1 paper. It is preserved here for reproducibility.

The full analysis pipeline (ingestion → cleaning → TLS 
construction → OLS regression → output) lives in /scripts/.

Do not modify this directory if you want to reproduce the 
exact paper results.
